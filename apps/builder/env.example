# apps/builder local environment variables

# API URL Configuration
# For local development: use full URL
VITE_API_URL=http://localhost:3000/api
# For Docker: use relative path /api (browser uses Vite proxy)
# VITE_API_URL=/api
# Proxy target for Vite dev server (Docker only - uses Docker service names)
# Only needed when VITE_API_URL is relative (/api)
# VITE_API_PROXY_TARGET=http://api-go:3000

# Public Assets CDN Configuration
# CDN base URL for default assets (images, layouts, music)
# For local development with MinIO:
VITE_PUBLIC_ASSETS_CDN_URL=http://localhost:9000/sacred-vows-public-assets-local

# Enable asset caching via Cache API (default: true)
VITE_ENABLE_ASSET_CACHING=true

# OpenTelemetry Configuration
# Set to false to disable observability entirely
VITE_OTEL_ENABLED=true
VITE_OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
VITE_OTEL_SERVICE_NAME=sacred-vows-web
VITE_OTEL_SERVICE_VERSION=unknown
VITE_OTEL_DEPLOYMENT_ENVIRONMENT=local
VITE_OTEL_TRACES_SAMPLER_RATIO=0.01
