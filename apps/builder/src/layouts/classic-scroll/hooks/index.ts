/**
 * Classic Scroll Layout Hooks
 * 
 * Layout-specific React hooks for the classic-scroll layout
 */

// Export hooks
import useEditable from './useEditable';

export { useEditable };

export default {
  useEditable,
};

